language: ruby
addons:
  postgresql: "9.3"
rvm:
  - 2.1.0
before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
test:
  adapter: postgresql
  database: travis_ci_test
  username: postgres
